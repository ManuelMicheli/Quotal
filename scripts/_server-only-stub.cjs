// Empty stub for the `server-only` package — used by the verify scripts so
// they can require modules that import 'server-only' outside the Next.js
// runtime (where the real package always throws).
module.exports = {}
